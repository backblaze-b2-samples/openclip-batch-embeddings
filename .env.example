# Backblaze B2 (S3-compatible API) — required
B2_APPLICATION_KEY_ID=your_key_id
B2_APPLICATION_KEY=your_application_key
B2_BUCKET_NAME=your-bucket-name
# Region is used to derive the S3 endpoint: https://s3.<region>.backblazeb2.com
B2_REGION=us-west-004

# Optional: explicit S3 endpoint override (defaults to the region-derived URL).
# B2_ENDPOINT=https://s3.us-west-004.backblazeb2.com

# Optional: public base URL for a PUBLIC bucket. When unset, corpus/result
# images stream via short-lived presigned URLs — the app runs fine without it.
# B2_PUBLIC_URL_BASE=https://your-bucket.s3.us-west-004.backblazeb2.com

# Example of what filled-in values look like (fake — for visual reference only):
# B2_APPLICATION_KEY_ID=00abcdef1234567000000001
# B2_APPLICATION_KEY=your-application-key-shown-once-on-create
# B2_BUCKET_NAME=my-embedding-corpus
# B2_REGION=us-west-004

# Frontend → API base URL. Defaults to http://localhost:8000 for local dev;
# set this to the deployed API origin in production (Next.js inlines it at
# build time, so it must be present when `pnpm build` runs).
# NEXT_PUBLIC_API_URL=https://your-api.example.com
